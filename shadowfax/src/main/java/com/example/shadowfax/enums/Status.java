package com.example.shadowfax.enums;

public enum Status {
	CHECKED_IN,          // Initial Status
    REACHED_LOCATION,    // When scanned at the delivery point
    OUT_FOR_DELIVERY     // When assigned to a Rider
}
